spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Projects/config-server
          #          username: mashrabbek
          #          password: dummy
          default-label: main
          clone-on-start: true
          force-pull: true